rest:
    host: localhost
    port: 8900
    endpoints:
        - name: AddOrder
          route: /order
          method: POST
          controller: minos.services.OrderService
          action: add_order
discovery:
    host: localhost
    port: 8080
    path: /discover
    endpoints: []
    db:
        host: localhost
        port: 6379
        password:
